\documentclass[border=6pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, decorations.pathreplacing, calc, fit}

\begin{document}
\begin{tikzpicture}[
  font=\small,
  >=Stealth,
  data/.style={draw, dashed, rounded corners=2pt, align=center, inner sep=4pt, fill=white},
  proc/.style={draw, rounded corners=2pt, align=center, inner sep=4pt, fill=gray!6},
  base/.style={proc, thick, fill=gray!12, text width=4.8cm},
  leverA/.style={proc, thick, text width=6.3cm, fill=orange!12, draw=orange!75!black},
  leverB/.style={proc, thick, text width=4.6cm, fill=teal!14, draw=teal!65!black},
  factbox/.style={proc, very thick, fill=gray!16, text width=8.6cm},
  evalb/.style={proc, text width=10.6cm},
  badgeA/.style={circle, fill=orange!82!black, text=white, font=\footnotesize\bfseries, inner sep=0pt, minimum size=5.5mm},
  badgeB/.style={circle, fill=teal!70!black, text=white, font=\footnotesize\bfseries, inner sep=0pt, minimum size=5.5mm},
  arr/.style={->, thick},
]

% ===== NODES =====
% ---- left column: data source + baseline ----
\node[data, text width=4.4cm] (bio) {\textbf{Biodiversity}\\[-1pt]{\footnotesize 6 classes $\cdot$ 2{,}143 tiles}};
\node[base, below=1.0cm of bio] (base) {\textbf{FT-UNetFormer baseline}\\[-1pt]{\footnotesize ADE20K-pretrained Swin encoder}};

% ---- the two crossed factors ----
\node[leverB, below=1.7cm of base] (lb) {\textbf{Class-balanced sampler}\\[-1pt]{\footnotesize settlement, semi-natural grassland}};
\node[badgeB, left=3mm of lb] {B};
\node[leverA, right=1.5cm of lb] (la) {\textbf{Cross-dataset transfer}\\[-1pt]{\footnotesize \textbf{Pre-train} on OpenEarthMap + Biodiversity (3{,}824 tiles)\\\textbf{Fine-tune} on Biodiversity (1{,}706 tiles)}};
\node[badgeA, right=3mm of la] {A};

% ---- OEM branch (top-right), right-aligned to the transfer box ----
\node[data, text width=3.6cm, anchor=north east] (oem) at (la.east |- bio.north) {\textbf{OpenEarthMap}\\[-1pt]{\footnotesize 8 classes $\cdot$ 3{,}500 tiles}};
\node[proc, text width=3.6cm, below=0.5cm of oem] (tax) {Rural filter\\+ harmonise};
\node[data, text width=3.6cm, below=0.45cm of tax] (oem6) {\textbf{OpenEarthMap}\\[-1pt]{\footnotesize 6 classes $\cdot$ 2{,}118 tiles}};

% ---- factorial cells: 2x2 mini-grid ----
\coordinate (mid) at ($(lb)!0.5!(la)$);
\node[factbox, below=1.8cm of mid] (fact) {%
  \textbf{$2\times2$ factorial:} transfer (\textcolor{orange!75!black}{\textbf{A}}) $\times$ sampler (\textcolor{teal!65!black}{\textbf{B}})\\[4pt]
  \begin{tabular}{r|c|c}
    \multicolumn{1}{c|}{} & {\footnotesize\textcolor{teal!65!black}{\textbf{B}} off} & {\footnotesize\textcolor{teal!65!black}{\textbf{B}} on}\\
    \hline
    {\footnotesize\textcolor{orange!75!black}{\textbf{A}} off} & baseline & sampler-only\\
    \hline
    {\footnotesize\textcolor{orange!75!black}{\textbf{A}} on} & transfer-only & \textbf{full}\\
  \end{tabular}%
};

% ---- evaluation ----
\node[evalb, below=0.6cm of fact] (eval) {\textbf{Evaluation}\; {\footnotesize 10 seeds $\cdot$ 219 validation tiles $\cdot$ 218 test tiles}};

% ===== ARROWS =====
\draw[arr] (bio) -- (base);
\draw[arr] (oem) -- (tax);
\draw[arr] (tax) -- (oem6);

% baseline forks into both factors
\coordinate (f) at ($(base.south)+(0,-0.7)$);
\draw (base.south) -- (f);
\draw[arr] (f) -| (lb.north);
\draw[arr] (f) -| ([xshift=-1.0cm]la.north);
% OEM (harmonised) feeds the transfer pre-training
\draw[arr] (oem6.south) -- (oem6.south |- la.north);

\draw[arr] (lb.south) -- (lb.south |- fact.north);
\draw[arr] (la.south) -- (la.south |- fact.north);
\draw[arr] (fact) -- (eval);

\end{tikzpicture}
\end{document}
